#!/bin/bash
# KB - Universal Knowledge Base Command
# Install this to /usr/local/bin/kb for system-wide access

KB_DAEMON_PATH="/Users/fathindosunmu/DEV/knowledge-base/.kb-daemon"

# Color codes for output
RED='\033[0;31m'
GREEN='\033[0;32m'
BLUE='\033[0;34m'
YELLOW='\033[1;33m'
NC='\033[0m' # No Color

# Main command handler
case "$1" in
    review)
        echo -e "${BLUE}📊 Opening KB Daily Review...${NC}"
        cd "$KB_DAEMON_PATH" && python3 interface/cli.py review
        ;;
    
    start)
        echo -e "${GREEN}🚀 Starting KB Daemon...${NC}"
        cd "$KB_DAEMON_PATH" && python3 kb_daemon.py start
        ;;
    
    stop)
        echo -e "${RED}⏹️  Stopping KB Daemon...${NC}"
        cd "$KB_DAEMON_PATH" && python3 kb_daemon.py stop
        ;;
    
    status)
        echo -e "${BLUE}📈 KB Daemon Status${NC}"
        if pgrep -f "kb_daemon.py" > /dev/null; then
            echo -e "${GREEN}✓ Daemon is running${NC}"
            echo ""
            ps aux | grep -E "kb_daemon|kb-daemon" | grep -v grep
        else
            echo -e "${YELLOW}○ Daemon is not running${NC}"
        fi
        
        # Show recent captures
        LATEST_LOG=$(ls -t "$KB_DAEMON_PATH/logs"/shadow_*.json 2>/dev/null | head -1)
        if [ -n "$LATEST_LOG" ]; then
            COUNT=$(grep -c '{' "$LATEST_LOG" 2>/dev/null || echo 0)
            echo ""
            echo -e "${BLUE}Recent captures: ${COUNT} events${NC}"
        fi
        ;;
    
    log|logs|view)
        echo -e "${BLUE}📋 KB Captured Data${NC}"
        cd "$KB_DAEMON_PATH" && python3 view_logs.py
        ;;
    
    capture)
        # Manual capture command
        shift
        echo -e "${GREEN}✏️  Manual KB Entry${NC}"
        echo "{\"type\":\"manual\",\"timestamp\":\"$(date -u +"%Y-%m-%dT%H:%M:%SZ")\",\"data\":{\"note\":\"$*\"},\"importance\":7}" >> ~/.kb-daemon/capture/manual_events.jsonl
        echo "Entry captured: $*"
        ;;
    
    test)
        echo -e "${BLUE}🧪 Testing KB System...${NC}"
        cd "$KB_DAEMON_PATH" && python3 kb_daemon.py test
        ;;
    
    stats)
        echo -e "${BLUE}📊 KB Statistics${NC}"
        cd "$KB_DAEMON_PATH" && python3 -c "
from storage.db_manager import DatabaseManager
from pathlib import Path
db = DatabaseManager(Path.home() / '.kb-daemon' / 'storage' / 'kb_store.db')
stats = db.get_statistics()
print(f'  Total Events: {stats[\"total_events\"]}')
print(f'  Avg Importance: {stats[\"average_importance\"]}/10')
print(f'  Pending Reviews: {stats[\"pending_entries\"]}')
print('\nTop Categories:')
for cat, count in sorted(stats['events_by_category'].items(), key=lambda x: x[1], reverse=True)[:5]:
    print(f'  - {cat}: {count}')
"
        ;;
    
    full|fullsync|sync-all)
        echo -e "${BLUE}🔄 Full Sync - Review + Graph Update${NC}"
        cd "$KB_DAEMON_PATH" && bash kb_full_sync.sh
        ;;
    
    sync)
        echo -e "${BLUE}📚 Syncing to knowledge graph...${NC}"
        cd /Users/fathindosunmu/DEV/knowledge-base
        if command -v uvx &> /dev/null; then
            uvx basic-memory sync
        else
            echo -e "${YELLOW}⚠️ uvx not found - install with: curl -LsSf https://astral.sh/uv/install.sh | sh${NC}"
        fi
        ;;
    
    graph|view)
        echo -e "${YELLOW}⚠️  No graph visualization command exists${NC}"
        echo ""
        echo "Basic Memory is an MCP server, not a visualization tool."
        echo ""
        echo "To explore your knowledge graph:"
        echo "  1. Ask Claude directly (it queries via MCP)"
        echo "  2. Use Obsidian for visual graph view"
        echo "  3. Search with: ${GREEN}kb query 'term'${NC}"
        ;;
    
    query)
        shift
        echo -e "${YELLOW}⚠️  Basic Memory doesn't have a direct query command${NC}"
        echo ""
        echo "To query your knowledge base:"
        echo ""
        echo "Option 1: Use Claude with MCP (recommended)"
        echo "  • Basic Memory runs as an MCP server"
        echo "  • Claude queries it directly through MCP"
        echo "  • Just ask Claude about your knowledge"
        echo ""
        echo "Option 2: Search markdown files directly"
        echo "  ${GREEN}grep -r '$*' .${NC}"
        echo ""
        echo "Searching for: $*"
        echo "-" * 40
        cd /Users/fathindosunmu/DEV/knowledge-base
        grep -r --include="*.md" -i "$*" . 2>/dev/null | head -20
        ;;
    
    list|entities)
        echo -e "${BLUE}📃 Listing knowledge base entries...${NC}"
        cd /Users/fathindosunmu/DEV/knowledge-base
        echo ""
        echo "Recent entries:"
        ls -lt **/*.md 2>/dev/null | head -20 || ls -lt *.md 2>/dev/null | head -20
        echo ""
        echo "Total: $(find . -name '*.md' -type f | wc -l) markdown files"
        ;;
    
    stats|info)
        echo -e "${BLUE}📊 Knowledge base statistics...${NC}"
        cd /Users/fathindosunmu/DEV/knowledge-base
        if command -v uvx &> /dev/null; then
            # Try to get stats
            echo "Markdown files: $(find . -name '*.md' -type f | wc -l)"
            echo "Directories: $(find . -type d -name '[!.]*' | wc -l)"
            echo ""
            echo "Recent entries:"
            ls -lt *.md 2>/dev/null | head -5
        else
            echo -e "${YELLOW}⚠️ uvx not found${NC}"
        fi
        ;;
    
    commit|sync-git|push)
        echo -e "${BLUE}📚 Syncing to Git...${NC}"
        cd "$KB_DAEMON_PATH" && bash git_sync.sh
        ;;
    
    backup|backups)
        shift
        case "$1" in
            list|"")
                echo -e "${BLUE}📦 Listing backups...${NC}"
                cd "$KB_DAEMON_PATH" && python3 backup_manager.py list
                ;;
            clean|rotate)
                echo -e "${YELLOW}🧹 Cleaning old backups...${NC}"
                cd "$KB_DAEMON_PATH" && python3 backup_manager.py rotate
                ;;
            restore)
                if [ -n "$2" ]; then
                    echo -e "${GREEN}🔄 Restoring backup: $2${NC}"
                    cd "$KB_DAEMON_PATH" && python3 backup_manager.py restore --backup "$2"
                else
                    echo -e "${RED}Please specify backup name${NC}"
                    echo "Usage: kb backup restore <backup_name>"
                fi
                ;;
            *)
                echo -e "${RED}Unknown backup command: $1${NC}"
                echo "Usage: kb backup [list|clean|restore <name>]"
                ;;
        esac
        ;;
    
    help|--help|-h|"")
        echo -e "${BLUE}🧠 KB - Knowledge Base Intelligence System${NC}"
        echo ""
        echo "A two-part system:"
        echo "  1. KB Daemon - Captures your development activity"
        echo "  2. Basic Memory - MCP server that Claude queries"
        echo ""
        echo "Usage: kb <command> [options]"
        echo ""
        echo "Commands:"
        echo "  === KB Daemon (Capture & Review) ==="
        printf "  ${GREEN}%-10s${NC} %s\n" "review" "Review and approve captured events"
        printf "  ${GREEN}%-10s${NC} %s\n" "start" "Start the KB daemon"
        printf "  ${GREEN}%-10s${NC} %s\n" "stop" "Stop the KB daemon"
        printf "  ${GREEN}%-10s${NC} %s\n" "status" "Check if daemon is running"
        printf "  ${GREEN}%-10s${NC} %s\n" "log" "View captured events"
        printf "  ${GREEN}%-10s${NC} %s\n" "capture" "Manual entry: kb capture 'learned X'"
        printf "  ${GREEN}%-10s${NC} %s\n" "stats" "Show capture statistics"
        echo ""
        echo "  === Knowledge Base (Search & Sync) ==="
        printf "  ${GREEN}%-10s${NC} %s\n" "sync" "Sync markdown to Basic Memory database"
        printf "  ${GREEN}%-10s${NC} %s\n" "query" "Search files: kb query 'search term'"
        printf "  ${GREEN}%-10s${NC} %s\n" "list" "List recent knowledge entries"
        echo ""
        echo "  === Workflow ==="
        printf "  ${GREEN}%-10s${NC} %s\n" "full" "🌟 Complete workflow (review + sync)"
        printf "  ${GREEN}%-10s${NC} %s\n" "commit" "📚 Commit changes to git"
        printf "  ${GREEN}%-10s${NC} %s\n" "backup" "📦 Manage backups"
        echo ""
        echo "Examples:"
        echo "  kb review              # Review today's captures"
        echo "  kb capture 'fixed auth bug'  # Manual entry"
        echo "  kb status              # Check if daemon is running"
        echo "  kb log                 # View recent activity"
        ;;
    
    *)
        echo -e "${RED}Unknown command: $1${NC}"
        echo "Use 'kb help' for available commands"
        exit 1
        ;;
esac
